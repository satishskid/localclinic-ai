{
  "name": "local-ai-medical-assistant",
  "version": "1.0.0",
  "description": "Local AI-powered medical assistant for ENT dental video analysis with privacy-first approach",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:webapp": "node scripts/build-webapp.js",
    "build:extension": "node scripts/build-extension.js",
    "download:models": "node scripts/download-models.js",
    "build:all": "npm run build:webapp && npm run build:extension",
    "server": "node server/index.js",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.1",
    "onnxruntime-web": "^1.17.0"
  },
  "devDependencies": {
    "archiver": "^6.0.1",
    "jsdom": "^27.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
